# Requests

Saved API request collections (Postman, Insomnia, `.http` files) used for manual testing and exploration during development.

- Keep collections in sync with the API as endpoints change - a stale collection is worse than none.
- Name files after the API/service they cover, e.g. `<service-name>.postman_collection.json`.
